---
title: 0042-接雨水
description: 计算柱子之间能接多少雨水，双指针/前后缀解法
theory_id: [THEORY-ALGO-SEARCH-01]
---

# 0042. 接雨水

## 题目描述

给定 `n` 个非负整数表示每个宽度为 1 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。

**示例：**

```
输入: height = [0,1,0,2,1,0,1,3,2,1,2,1]
输出: 6
                     __
         __          | |__    __
    __  | |__    __  | | |__| |__
___| |__| |__|__| |__| | |__| |__|___
```

## 思路分析

### 核心公式

当前位置能接的雨水量：

```
ans[i] = min(leftMax[i], rightMax[i]) - height[i]
```

其中 `leftMax[i]` 是 i 左边（含 i）的最高柱子，`rightMax[i]` 是 i 右边（含 i）的最高柱子。

### 方法一：前后缀数组

```
1. left[i] = max(left[i-1], height[i])   遍历一次
2. right[i] = max(right[i+1], height[i])  遍历一次
3. ans += min(left[i], right[i]) - height[i]  遍历一次
```

### 方法二：双指针（空间 O(1)）

``` 
用两个变量 maxL 和 maxR 代替数组，双指针从两端向中间移动：
- 如果 height[l] < height[r]：左边是短板，计算左边并移动左指针
- 否则：右边是短板，计算右边并移动右指针
```

## C++ 代码

```cpp
// 方法一：前后缀数组
class Solution {
public:
    int trap(vector<int>& height) {
        int n = height.size();
        if (n == 0) return 0;
        
        vector<int> left(n), right(n);
        left[0] = height[0];
        for (int i = 1; i < n; i++)
            left[i] = max(left[i - 1], height[i]);
        
        right[n - 1] = height[n - 1];
        for (int i = n - 2; i >= 0; i--)
            right[i] = max(right[i + 1], height[i]);
        
        int ans = 0;
        for (int i = 0; i < n; i++)
            ans += min(left[i], right[i]) - height[i];
        return ans;
    }
};
```

```cpp
// 方法二：双指针（空间优化）
int trap(vector<int>& height) {
    int n = height.size();
    int l = 0, r = n - 1;
    int maxL = 0, maxR = 0, ans = 0;
    
    while (l < r) {
        maxL = max(maxL, height[l]);
        maxR = max(maxR, height[r]);
        
        if (maxL < maxR) {
            ans += maxL - height[l];  // 左边界是短板
            l++;
        } else {
            ans += maxR - height[r];  // 右边界是短板
            r--;
        }
    }
    return ans;
}
```

## 复杂度分析

| 方法 | 时间复杂度 | 空间复杂度 |
|------|:---------:|:---------:|
| 前后缀数组 | $O(n)$ | $O(n)$ |
| 双指针 | $O(n)$ | $O(1)$ |

## 易错点 / 拓展思考

- ❗ 前后缀中 `left[i]` 和 `right[i]` 包含 `height[i]` 本身，所以 `min(left,right) - height[i]` 不会为负
- ❗ 双指针法中 `maxL < maxR` 时计算左边，否则计算右边，不能搞反
- ❗ 双指针法不需要分别计算左右两边的 max，而是动态维护全局的 maxL 和 maxR
- **拓展 1**：如果柱子宽度不统一（不是 1），如何调整公式？
- **拓展 2**：二维接雨水（LeetCode 407）需要用优先队列 BFS 求解
